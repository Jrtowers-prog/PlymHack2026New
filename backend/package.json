{
  "name": "safe-night-home-backend",
  "version": "2.0.0",
  "description": "Safe Night Home — 2-service backend (API Gateway + Safety Compute)",
  "scripts": {
    "start:gateway": "node src/gateway/server.js",
    "start:safety": "node src/safety/server.js",
    "dev:gateway": "node --watch src/gateway/server.js",
    "dev:safety": "node --watch src/safety/server.js",
    "dev": "node --watch src/gateway/server.js & node --watch src/safety/server.js",
    "start": "node src/gateway/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0"
  }
}
