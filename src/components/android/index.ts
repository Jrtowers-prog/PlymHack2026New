/**
 * Android-specific component infrastructure.
 *
 * AndroidOverlayHost creates a separate compositing layer with high elevation
 * so all UI overlays render above native Android map views.
 */
export { AndroidOverlayHost } from './AndroidOverlayHost';
